{
    "name": "deloitte-rfp-command-center",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "vercel-build": "cd frontend && npm install && chmod +x node_modules/.bin/vite && npm run build"
    }
}